from setuptools import setup

version = "0.1.0"

setup(
    name='OSMParser',
    version=version,
    author='Graham Coombe',
    author_email='gscoombe@gmail.com',
    install_requires=[
        "requests==2.10.0"
    ]
)
